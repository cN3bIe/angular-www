Переписал на компоненты - используй angular.component, а не .controller, сделай приложение деревом компонентов, как в реакте

Профайл просто наследует регистрацию, так правильно? не совсем, профайл использует регистрацию для отображения и редактирования данных, то есть темплейт у них один, а контроллера 2. Можно и через наследование реализовать. Сейчас у тебя получается так, что в профайле и регистрации практически одинаковые теплейты, только в профайле появился аттрибут readonly, их можно объединить в один. А по коду, если я на профайле нажму на кнопку Update она ведь сразу вызовет submit, хотя мы еще не вносили изменений никаких, только убрали readonly

чтобы избежать
constructor( $state, $log,contryREST ){
  this._$log = $log;
  this._$state = $state;
  this._contryREST = contryREST;
 }
и
RegistrationForm.$inject = [ '$state', '$log','contryREST'];

можно класс твоего компонента обернуть в обычную функцию, в которую ты заинжектишь нужные зависимости и вернешь из нее экземпляр класса. Тогда зависимости можно будет напрямую использовать, без необходимости писать из в this. Так удобнее имхо, особоенно, когда зависимостей 10-15